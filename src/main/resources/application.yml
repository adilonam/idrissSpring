# DataSource configuration
spring:
  datasource:
    url: jdbc:mysql://localhost:3306/TripApi
    username: root
    password: 2641
    driver-class-name: com.mysql.cj.jdbc.Driver

# JPA configuration
  jpa:
    database-platform: org.hibernate.dialect.MySQLDialect
    hibernate:
      ddl-auto: update
    show-sql: true
